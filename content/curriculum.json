{
  "schemaVersion": 1,
  "baseline": {
    "id": "kotlin-jvm-2026-09",
    "kotlinCompiler": "2.4.20",
    "languageVersion": "2.4",
    "apiVersion": "2.4",
    "jdk": "Eclipse Temurin 25.0.1+8-LTS",
    "jvmTarget": "21",
    "gradle": "9.7.0",
    "coroutines": "1.11.0"
  },
  "categories": [
    {
      "id": "type-system",
      "name": "Type system",
      "description": "Kotlin types, null safety, and the values that make invalid states explicit.",
      "color": "#7c3aed",
      "symbol": "?"
    },
    {
      "id": "java-interoperability",
      "name": "Java interoperability",
      "description": "Kotlin boundaries with Java APIs, types, and nullability contracts.",
      "color": "#0f766e",
      "symbol": "J"
    }
  ],
  "studyPaths": [
    {
      "id": "java-developer-foundations",
      "name": "Java developer foundations",
      "description": "A curated path through the Kotlin concepts that matter first to an experienced Java developer.",
      "conceptIds": [
        "nullable-types",
        "platform-types",
        "not-null-assertion"
      ],
      "scenario": {
        "title": "Stabilize a Java customer boundary",
        "context": "A Kotlin service calls a legacy Java CustomerApi. Its unannotated findDisplayName method is used to build a response required by a downstream Java client.",
        "stages": [
          {
            "id": "predict-boundary",
            "kind": "Prediction",
            "prompt": "What Kotlin type and runtime behavior should you expect when findDisplayName returns null, but its Java signature only says String?",
            "feedback": "The call enters Kotlin as the platform type String!. Kotlin lets you use it as String or String?, so an immediate non-null assignment can compile and then fail at runtime when Java returns null.",
            "nextConstraint": "Production traces now show that null means the customer deliberately has no public display name. The API boundary must preserve that domain fact."
          },
          {
            "id": "diagnose-contract",
            "kind": "Diagnosis",
            "prompt": "Where should nullability be made explicit, and why is waiting for a later dereference too weak?",
            "feedback": "Normalize the platform type at the Java boundary, for example as val displayName: String? = api.findDisplayName(id). That gives the rest of the Kotlin code an honest nullable contract and forces handling near the source.",
            "nextConstraint": "A teammate proposes displayName!! because the downstream response still requires a non-null label. You must choose a deliberate fallback or assertion policy."
          },
          {
            "id": "design-response",
            "kind": "Design choice",
            "prompt": "Would you use !!, a fallback, or reject the response? State the invariant and operational trade-off behind your choice.",
            "feedback": "Use !! only when an independently enforced invariant makes null impossible and a crash is the intended failure mode. Here null is valid domain data, so an explicit fallback or a modeled rejection keeps the policy visible and testable.",
            "nextConstraint": "The boundary, nullable domain value, and response policy now need to be reviewed as one design rather than three isolated syntax choices."
          }
        ],
        "debrief": {
          "connections": "Platform types explain why Kotlin initially trusts the Java signature; nullable types express the boundary fact inside Kotlin; the not-null assertion is a policy choice with a failure mode, not a conversion technique.",
          "tradeOffs": "Normalizing to String? adds handling work but localizes uncertainty. A fallback keeps the request alive but may hide missing data. Rejecting is explicit but reduces availability. An assertion is shortest but turns a known valid null into a crash.",
          "rubric": [
            "Identifies the unannotated Java result as a platform type and predicts the runtime risk.",
            "Moves uncertainty into an explicit nullable type at the interoperability boundary.",
            "Chooses a response policy by naming its invariant, user impact, and failure mode.",
            "Explains how the three decisions reinforce one coherent boundary design."
          ]
        }
      }
    }
  ]
}
